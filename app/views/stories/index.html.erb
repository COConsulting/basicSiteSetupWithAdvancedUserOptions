<% @title="Stories" %>
<p id="notice"><%= notice %></p>

<h1>Stories</h1>
<p>Below are the stories submitted to us about others like yourself who are making a difference in a person's life, one bag at a time.</p>
<table>
  <thead>
    <tr>
      <th>Submitted by</th>
      <th>Heading</th>
      <th>Story</th>
    </tr>
  </thead>

  <tbody>
    <% @stories.each do |story| %>
      <% if story.published %>
        <tr>
          <td><%= story.authorName %></td>
          <td><%= link_to story.heading, story %></td>
          <td><%= story.body %></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Tell us your story', new_story_path %>